Document retrieval feature:

1. In the current state, retrieve all uploaded documents from the database using get request
    a. Should be running in the background asychronously whenever there is an upload to the database
2. Render that in a table